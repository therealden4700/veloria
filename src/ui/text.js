// Переходник: интерфейс зовёт `text`, `measure`, `wrap` — теперь они векторные.
//
// Имена оставлены прежними намеренно. Экраны интерфейса — четыре сотни вызовов
// отрисовки; переписывать их на новые имена значило бы перетряхнуть 2300 строк
// ради переименования, а вся разница в том, чем рисуется буква. Мир при этом
// продолжает звать `art/text.js` и остаётся пиксельным.

export {
  uiText as text,
  uiMeasure as measure,
  uiWrap as wrap,
  uiBlock as textBlock,
  uiEllipsize as ellipsize,
} from './type.js';
